{
  "name": "day-api",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bunx prisma generate && bun build --compile --minify-whitespace --minify-syntax --target bun --outfile server ./src/index.ts",
    "start": "./server"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "@prisma/client": "^6.13.0",
    "better-auth": "^1.3.4",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^6.13.0"
  },
  "module": "src/index.js"
}
